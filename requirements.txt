# Orquestração da infraestrutura Fog/Fogbed
fogbed>=1.3.0

# Mininet para emulação de rede (Fogbed depende disso)
mininet

# Cliente Docker em Python (Fogbed usa docker-py por baixo)
docker>=6.0.0

# HTTP para chamadas RPC IOTA
requests>=2.28.0

# YAML para manipular configs do genesis/validator
pyyaml>=6.0

# Utilidades de rede (opcional, mas útil)
netaddr
networkx
