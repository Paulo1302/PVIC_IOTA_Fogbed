# Orquestração da infraestrutura (Fog/Cloud)
fogbed>=1.5.0

# Biblioteca de emulação de rede
mininet

# Cliente Docker para Python (usado pelo Fogbed)
docker>=6.0.0

# Ferramentas auxiliares de rede (opcional, mas recomendado)
netaddr
networkx